from setuptools import setup, find_packages

setup(
    name="mosaic",
    version="0.1.0",
    description="Mosaic - simple universal ORM (MVP, SQLite-first)",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)
